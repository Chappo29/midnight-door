# Фикс-пасс «Bug / Reliability» — итоги

**Дата:** 2026-09-25 · **Ветка:** `main`, коммиты `a014d3b…cc45b66` · **Источник задач:** [GAME_AUDIT.md](GAME_AUDIT.md)
(уточнения фиксов — [GAME_AUDIT_RESEARCH.md](GAME_AUDIT_RESEARCH.md))

Цель прохода: стабильная baseline-версия для теста на детях **без изменений** баланса, экономики, цен, сложности,
длительности матча, наград, core loop и состава построек. Ничего из этого не менялось.

> **Статус: регрессионная проверка нашла новые Blocker/Critical (см. «Проблемы, найденные регрессией»).**
> Все они — старые дефекты, а не последствия правок этого прохода (разбор ниже). По договорённости они
> **не исправлены** до решения владельца.

---

## Что исправлено

Каждая задача: воспроизведение на текущем коде → минимальная правка → регрессионный тест (где разумно) →
`vitest` + `tsc` → проверка в живой игре (headless Chromium через playwright-cli).

| # | Задача | Коммит | Что было | Что сделано |
|---|---|---|---|---|
| 0 | Карточка «Тебя поймали» (из параллельной сессии) | `a014d3b` | Незакоммиченная правка, в которой жил B1 | Закоммичена отдельно, как есть (решение владельца) |
| 1 | **B1** зависание после «Поймали → Выйти в меню» + **п.10** сброс состояния сцены | `ae7705f` | `caughtPaused` переживал сцену: новый матч стоял (`time +0` за 3 с) | `init()` сбрасывает **все** поля сцены с начальным значением (ещё `tut*`, `menuBuild`, `drag`, `pinch`, `ghost*`, `downAt`). Шапка-комментарий «новое поле — добавь сюда» |
| 2 | **B3** «Бу!»/«Искорка» без звука и эффекта | `47b5fc8` | События команд между тиками стирались в начале `step()` | `step()` переносит события, добавленные командами между тиками, в события тика; ровно один раз |
| 3 | **B6** петля обучения на шаге пушки | `b77ea4a` | Повторный тап по пальцу = «иди сюда» → отмена стройки → меню снова | Пока игрок идёт строить/улучшать/чинить, палец убран и тапы по полю не проходят |
| 4 | **B10** кот/палец поверх паузы, подсказка поверх итогов · **B15** HUD поверх окон | `871bd74` | На паузе палец показывал на «Выйти в меню», облачко закрывало «Играть»; фантики/портреты поверх карточек | При открытом окне оверлеи обучения и подсказок спрятаны; подсказка сбрасывается в конце матча; игровой HUD скрыт классом `screen-open` |
| 5 | **B7 / UI1** `#ui button` сбрасывал вид всех кнопок | `452a6b5` | «Ещё раз», «Играть», «Забрать» — Nunito 16px 400 тёмным; «Лёгкая»/«Сложная» — белым | Сброс шрифта/цвета перенесён в `:where(#ui) button` (нулевая специфичность), `pointer-events` оставлен на сильном селекторе (уточнение из исследования). Все экраны перепроверены скриншотами |
| 6 | **B2** кнопки итогов вне экрана на телефоне в альбоме | `23f4cc2` | `.halftone { overflow:hidden }` отключал прокрутку карточки | Точки фона — в `background`; карточка прокручивается (`92dvh`); на высоте ≤500/≤400 px итоги и поимка компактнее — кнопки видны **без прокрутки** |
| 7 | **B5** сквозные клики / двойной тап | `d72224a` | Двойной «Ещё раз» выбирал комнату; 6 кликов по подарку запускали обучение; меню постройки открывалось под пальцем | (а) 350 мс после смены экрана/открытия меню клики по UI глотаются; тап по полю — только если жест начался после смены экрана. (б) Фильтр повторного тапа ребёнка (1 с, 60 px — исследование N15) для пунктов меню и для поля. (в) В портрете меню ставится над/под пальцем. (г) После «Забрать» 1 с до «Отлично!» |
| 8 | **Top-10(a)** пустой экран при загрузке | `cc45b66` | До 11 с (10 Мбит/с) пустого экрана при первом запуске и после «Играть» | Заставка в `index.html` (дверь, название, полоска по `load.progress`) видна до первого экрана и снова — если матч запускают до конца загрузки |

## Какие regression tests добавлены

Итого: **90 → 103** теста, 11 файлов, все зелёные; `tsc --noEmit` без ошибок. Каждый новый тест проверен на
«красный без правки → зелёный с правкой».

| Тест | Файл | Что ловит |
|---|---|---|
| `test_scene_init_resets_every_stateful_field` | `tests/scene-reset.test.ts` | B1 и весь класс «поле сцены не сброшено в `init()`» — сверяет список полей с телом `init()` |
| `test_spirit_boo_event_survives_until_next_step` | `tests/spirit.test.ts` | B3: событие команды между тиками доходит ровно один раз |
| `test_tutorial_repeated_taps_on_finger_do_not_cancel_cannon_build` | `tests/tutorial.test.ts` | B6: «ребёнок» жмёт на палец раз в 0,7 с — шаг пройден < 5 с |
| `test_tutorial_hides_finger_while_player_builds` | `tests/tutorial.test.ts` | B6: во время стройки пальца нет, тап не проходит |
| `test_hints_current_hint_disappears_when_match_ends` | `tests/hints.test.ts` | B10: подсказка не живёт поверх итогов |
| `test_ui_button_reset_does_not_override_button_classes` | `tests/css-rules.test.ts` | UI1: в `#ui button` нет `font`/`color` |
| `test_result_card_stays_scrollable_on_low_screens` | `tests/css-rules.test.ts` | B2: `.card` прокручивается, `.halftone` без `overflow` |
| 4 теста `test_input_guard_*` | `tests/input-guard.test.ts` | B5: повторный тап 0,85 с, намеренный тап не глотается, меню в портрете не накрывает палец |
| 2 теста `test_boot_screen_*` / `test_game_start_*` | `tests/boot-screen.test.ts` | Top-10: заставка в HTML до бандла, старт матча не ждёт ассеты на пустом экране |

Ограничение: B1, B2, B5, B7, B15 и заставка — поведение браузера; юнит-тесты стерегут причину (исходник/CSS),
а само поведение подтверждено в живой игре (ниже). Отдельного e2e-набора в проекте пока нет.

## Что проверено вручную (живая игра)

Headless Chromium (playwright-cli), dev-сервер. ⚠ — состояние подготовлено перемоткой/подменой, ввод настоящий.

- **B1:** поимка ⚠ → «Выйти в меню» → «Лёгкая»: было `time +0 / caughtPaused true`, стало `+3 с / false`.
- **Цепочка перезапусков (п.10):** обучение → пауза → меню → матч → итоги ⚠ → «Ещё раз» → пауза → меню → повтор обучения: в каждом матче время идёт, флаги сброшены, лишних оверлеев нет.
- **B3:** дух ⚠ жмёт «Бу!» — звучит `sfx_ghost_boo`, призрак застывает.
- **B6:** живое обучение: после «Пушка» ещё 3 тапа по клетке — проигнорированы, стройка не отменена, шаг пройден за 2,8 с.
- **B10/B15:** пауза в обучении на 844×390 — кота/пальца нет; карточка поимки — HUD скрыт (скриншоты до/после).
- **UI1:** computed style до/после («Ещё раз» → Rubik 19px 900 #fff); скриншоты меню, итогов, подарка, магазина, меню постройки.
- **B2:** кнопки видны без прокрутки на 667×375, 740×360, 844×390, 915×412, 1024×500, 1280×720, 390×844 (поражение/победа/командная); на 640×320 — прокрутка работает. Карточка поимки на 740×360 — помещается.
- **B5:** двойной клик «Ещё раз» с паузой 200/400/700/900 мс — комната не выбирается; 6 кликов по «Забрать» — обучение не запускается; двойной тап по клетке в портрете — ничего не куплено; обычный выбор «Пушки» — строится.
- **Заставка:** эмуляция 4 Мбит/с — 45 с загрузки без единого пустого кадра; первый запуск — заставка → обучение; меню открыто → «Лёгкая» до конца загрузки → заставка → матч.

## Регрессионная проверка вслепую

4 независимых агента без списка исправлений (не читали аудит и git-историю), по сценариям: первый запуск, обучение,
первый матч, поимка, дух, выход в меню, новый матч, поражение, победа, «Ещё раз», portrait/landscape, быстрые/двойные тапы.
Отчёты: `scratchpad/regression/r1…r4-report.md` (временная папка сессии).

| Агент | Итог |
|---|---|
| R1 Functional QA | **Blocker/Critical нет.** Все сквозные сценарии проходятся; монеты/победы/усилители/подарок/сохранение сходятся (+55 победа, +40 командная, +31 поражение, +60 за обучение один раз); консоль чистая (кроме `favicon.ico` 404) |
| R2 Mobile / Touch QA | 1 Blocker, 2 Critical, 1 High (ниже). Двойные тапы по клеткам/меню ничего не покупают; «Забрать», «Ещё раз», «Лёгкая», «Играть духом» устойчивы; все кнопки всех экранов видны на 4 размерах телефона |
| R3 Tutorial QA | 1 Blocker (ниже). Пауза прячет кота; ⏭ только удержанием; двойной «Играть» после обучения не выбирает комнату; послушный ребёнок проходит за ~54 с |
| R4 State / Architecture QA | 1 Critical (ниже). Утечек за десятки перезапусков нет (слушатели, DOM, объекты сцены, твины); время/пауза/ESC работают; «Бу!»/«Искорка» не теряются и не дублируются; visibilitychange — корректно |

### Появились ли regression bugs

**Регрессий, вызванных правками этого прохода, не найдено** (по коду: ни одна находка не проходит через изменённые
места так, чтобы правка её создала; поведение до правок в браузере отдельно не перепроверялось — это strong hypothesis).
Найдены **старые** дефекты, которые правки не закрыли или не затрагивали.

### Проблемы, найденные регрессией (открыты, ждут решения)

| Sev | Проблема | Кто нашёл | Откуда | Предлагаемая техническая правка |
|---|---|---|---|---|
| **Blocker** | После пропуска обучения удержанием ⏭ тапы по полю не работают до перезагрузки (касание ⏭ «висит», каждый тап = щипок) | R2 | старое; механизм «ладонь у края = щипок» из аудита (TR/CTRL) | Сбрасывать «зажатые» указатели при смене сцены/пропуске; щипок только при двух касаниях **по полю** |
| **Blocker** | Обучение навсегда застревает на «Чини дверь!», если ребёнок ~35–50 с не жмёт ключ: удары на паузе, ящик с инструментами лечит дверь до полной, ключ отвечает «Дверь целая» | R2, R3 | старое; продолжение Top-5 аудита | На шаге `repair` не давать двери вылечиться до полной (или считать шаг пройденным, если дверь снова целая); стартовать шаг при HP < 60 % |
| **Critical** | Двойной тап по «Выйти в меню» (пауза/поимка) с интервалом 400–800 мс запускает «Кошмар»/«Сложную» и списывает усилители | R4, R2 | старое; B5 закрыт не до конца (для кнопок нового окна только 350 мс) | Фильтр повторного тапа (1 с, 60 px) — и для кнопок только что открытого окна |
| **Critical** | Поворот телефона: поле остаётся в размере прошлой ориентации, половина экрана чёрная, переходит в следующий матч | R2, R1 (и №06 аудита) | старое, **B9** — вне этого прохода | `getParentBounds()` + `scale.refresh()` с задержкой (rAF + 150 мс) на resize/orientationchange/visualViewport (баг Phaser #7213) |
| High | Частые тапы по 🔧 перезапускают починку (1,2 с) — у нетерпеливого ребёнка шаг 51 с вместо 4 с; то же в матче | R3 | старое | Повторный `repair` во время починки игнорировать |
| High | `hints` в сохранении не массив → пустой экран/замирание, перезагрузка не помогает | R4 | старое, **B8** — вне прохода | `normalizeProgress` |
| Medium | Магазин, «Усилители»: медленный двойной тап (500 мс) покупает дважды | R1 | старое, часть B5 | Тот же фильтр повторного тапа |
| Medium | За 1,5 с после смерти призрака пауза → «Выйти в меню» теряет победу и монеты | R4 | старое | Пауза недоступна, когда исход уже есть |
| Medium | Палец/ладонь у края экрана — все тапы по полю считаются щипком | R2 | старое (аудит №06) | Вместе с Blocker ⏭ |
| Low | Шаг «Чини дверь!» открывается при целой двери — первый правильный тап даёт «Дверь целая» + звук ошибки | R3 | старое, Top-5 аудита | Вместе с Blocker обучения |
| Low | Тап со сдвигом > 12 px теряется; тап в момент победы — «Игра окончена» + звук ошибки; выход из первого обучения не запоминается; «Загляни в магазин!» без кнопки | R1–R3 | старое | — |

## Задачи из GAME_AUDIT.md, которые можно считать закрытыми

| Задача | Статус |
|---|---|
| B1 зависание после поимки | **Закрыта** |
| B3 события духа | **Закрыта** |
| B6 петля обучения на пушке | **Закрыта** (но см. новый Blocker на шаге ремонта — отдельная задача Top-5) |
| B7 / UI1 стиль кнопок | **Закрыта** |
| B10 оверлеи поверх паузы/итогов | **Закрыта** |
| B15 HUD поверх окон | **Закрыта** |
| B2 итоги на телефоне в альбоме | **Закрыта** (кроме экранов ниже ~340 px — там прокрутка) |
| Top-10(а) заставка вместо пустого экрана | **Закрыта** (пункты б–г Top-10 — лишние SFX, ленивая музыка, атласы — не делались) |
| B5 сквозные клики / двойной тап | **Частично**: поле, меню постройки, подарок, «Ещё раз», «Играть духом» — закрыты; кнопки нового окна при интервале 400–800 мс («Выйти в меню» → сложность) и магазин — открыты |
| TR7 (сброс состояния сцены) | **Закрыта** вместе с B1 |
| TR8 (очередь событий sim → view) | **Закрыта** в минимальном виде (перенос событий команд в следующий тик) |

Не трогались (вне прохода): B4, B8, B9, B11–B14, Top-4 и остальные Top-5…9, все UX/GD/EN/FR-пункты.

---

## Дополнение: исправления после регрессии (с разрешения владельца)

Все 4 Blocker/Critical из таблицы выше исправлены, каждое воспроизведено до правки и проверено после:

| Проблема | Коммит | Проверка |
|---|---|---|
| Blocker: после пропуска ⏭ удержанием тапы по полю не работали (касание «зависало» в Phaser) | `d67bc28` | CDP-тач: удержание 1,6 с → тап выбирает комнату; щипок считается только по касаниям на поле |
| Blocker: обучение навсегда застревало на «Чини дверь!» (ящик лечил дверь до полной) | `961cf04` | Тест: 90 с ожидания → ключ → следующий шаг; вживую 60 с ⚠ → дверь 438/800 → ключ → «Улучши пушку» |
| Critical: двойной тап «Выйти в меню» запускал «Кошмар»; магазин продавал дважды | `90c3be2` | 390×844 тапы 500/600/800 мс → просто меню; магазин ×2 через 500 мс → одна покупка |
| Critical: поворот телефона — полэкрана чёрное (B9) | `6f8e6cd` | Эмуляция 390×844 ↔ 844×390 ×4 — размер поля совпадает с окном; на устройстве не проверено |

**Повторная проверка вслепую** (R2 и R4 после правок; R3 остановлен — обучение заново проверено в Child UX Pass):
- R2: все 4 перепроверенные находки исправлены (поворот — 17 проверок подряд; «Выйти в меню» — 15 комбинаций);
  палец на поле у края — частично; тап со сдвигом > 12 px — закрыт позже (`66c4054`, Child UX Pass).
- R4: Critical исправлен (тач и мышь, 5 размеров), утечек и регресса нет; намеренные быстрые действия не глотаются.
- Остались (Low): защита от повтора ровно 1 с — двойной тап с промежутком ~950 мс снова может попасть в новое окно;
  на ПК клик по паузе в течение ~1 с после ESC глотается.

Итого после фикс-пасса: B1, B2, B3, B5, B6, B7/UI1, B9, B10, B15, Top-10(а), TR7, TR8 закрыты. B4/Top-4 (выход и усилители),
Top-5 (ремонт в обучении), B13, B14 закрыты позже в Child UX Pass — см. [CHILD_UX_PASS.md](CHILD_UX_PASS.md).
**B8 (битое сохранение) не закрыт.**
